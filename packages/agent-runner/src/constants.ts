export const MAX_OUTPUT_LINES = 500;
export const FALLBACK_PROGRESS_WINDOW_MS = 60_000;
export const SUPPORTED_ENGINES = ['claude', 'codex', 'cursor'] as const;
export const SUPPORTED_MODELS = ['opus', 'sonnet', 'haiku'] as const;
